function check() {
	a.innerHTML = "<p>hello</p>";
	window.plugin.email.isServiceAvailable(
		var a = document.getElementById("mylink");
		a.onClick = function (isAvailable) {
			alert(isAvailable ? 'Service is available' : 'Service NOT available');
			a.innerHTML = "<p>hi</p>";
			return false;
		}
	);
}